from django.urls import path
from .apis.{{app_name}} import {{app_name}}_list, {{app_name}}_detail


urlpatterns = [
    path('api/v1/{{app_name}}/{{app_name}}', {{app_name}}_list,
    path('api/v1/{{app_name}}/{{app_name}}/<int:pk>', {{app_name}}_detail,
]
